// for coding quiz
var counter = document.getElementsByClassName("timetest");
var startButton = document.getElementsByClassName("start")
console.log(startButton);

addEventListener("click", )//working
// startButton

// counter[0].innerHTML = "Replaces placeholder."; this works, but..

counter[0].textContent = "Replaces placeholder.";
var interval = setInterval(function(){

},1000);